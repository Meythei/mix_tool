# パッチノート

## 2026-09-03 — ハーモニックミキシング + Hot Cue + ローカルAI Mix Assistant

Ableton(打ち込み)× rekordbox(ライブラリ機能)の「いいとこ取り」をさらに一歩進め、rekordbox側の機能を3つ追加しました。バックエンドの解析結果を拡張しただけの部分と、UIに新しいパネル/コントロールを追加した部分があります。

### 追加機能

- **Camelotキー表記(ハーモニックミキシング)**
  - ライブラリ解析で推定したキー(例: `A minor`)を、rekordbox/Mixed In Keyでおなじみの Camelot コード(例: `8A`)に自動変換。
  - ライブラリ項目に色分けされたキーチップとして表示(色相はCamelotの時計盤(1〜12)にマッピング)。
  - `backend/analysis.py: key_to_camelot()`

- **トラックEnergy(1〜10)**
  - 音量(RMS)とBPMから簡易的な「エネルギー感」を1〜10で算出し、ライブラリ項目にバー表示。
  - あくまでヒューリスティックな目安(rekordboxの正確なアルゴリズムの再現ではありません)。
  - `backend/analysis.py: _estimate_energy()`

- **Hot Cue(rekordbox風)**
  - ライブラリの各トラックに波形クリックで試聴 → 8つまでのHot Cueを打ち込み保存(丸ボタン1〜8)。
  - 打ったCueをクリックすると再生位置にジャンプ&試聴、さらにそのトラックをアーム状態にして「Cue位置から」クリップを配置できるようにプライム(強調表示)。
  - 右クリックでCue削除。Cueはライブラリキャッシュに保存され、ファイルの再解析(BPM/キーの更新)をまたいで保持されます。
  - バックエンド: `POST /api/library/cues/set`, `POST /api/library/cues/clear`, `GET /api/library/audio`(スキャン済みパスのみ配信・パストラバーサル対策込み)
  - フロント: `frontend/app.js` のライブラリ項目レンダリング・共有プレビュー`<audio>`・`placeClipFromLibrary()`のオフセット適用

- **Mix Assistant(ローカルAI/オフライン・ヒューリスティック)**
  - アーム中のトラック(またはクリップ選択中のソース)に対して、Camelot相性(同キー/関係調/±1/±2)・BPM近似・エネルギーの流れをスコアリングし、ライブラリから上位5件を提案。
  - 完全にブラウザ内で完結する決定的なルールベース処理(モデルのダウンロードやネットワーク通信は一切なし)。オフライン前提のツールという性質上、「Gemini Nano程度のローカルAIで十分」という方針に対し、実際のLLM/軽量モデルではなく、同等の役割を果たす軽量な自前ヒューリスティックとして実装しています。
  - 候補をクリックするとそのままアーム。
  - `frontend/app.js: scoreCandidate()`, `renderMixAssistant()`

### 既知の制約(追加分)

- Energy値とCamelotキーは音源の推定値です。特にキー判定はBPM推定と同様に外れることがあります。
- Mix Assistantはルールベースの近似であり、実際の楽曲の相性を保証するものではありません。

### 検証

- `scripts/smoke_test.py` を実行し解析→レンダリングの一気通貫を確認。
- サーバーを起動し、Playwrightヘッドレスブラウザで実ブラウザ動作を確認:
  - ライブラリ項目にCamelotチップ・Energyバー・Hot Cueパッドが表示されること
  - 空のCueパッドクリックでCue作成、既存Cueクリックでプライム(ハイライト)されること
  - プライムした状態でデッキレーンをクリックすると、配置されたクリップの`source_offset`がCue位置と一致すること
  - トラックをアームするとMix Assistantに5件の候補が表示されること
  - 既存のPreview/Export機能に影響がないこと
  - コンソールエラーが発生しないこと
- 上記すべて確認済み。
