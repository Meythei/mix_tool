# パッチノート

## 2026-09-03 — ハーモニックミキシング + ローカルAIミックスアシスタント + ホットキュー

rekordbox が持っていて DJ Mix Studio に無かった機能を3つ追加。すべてオフラインで完結し、外部サービス/モデルのダウンロードなし。

### 追加

- **Camelotキー表記(ハーモニックミキシング)**
  ライブラリ解析で検出したキー(例: "A minor")に加えて、Camelot Wheel表記(例: "8A")を計算して表示するようになった。ライブラリ項目のキー詳細がキー同士の色分けバッジになり、ひと目で相性を判断しやすくした(`backend/harmonic.py`)。

- **トラックエナジー評価(1〜10)**
  RMSラウドネスから簡易的なエナジー値を算出し、ライブラリに⚡チップとして表示。静かな曲とピークタイムのバンガーを見分ける手がかりに(既存の解析キャッシュにこの値が無い古いエントリは、次回スキャン時に自動で再解析される)。

- **AI Mix Assistant(ローカルAI・オフライン動作)**
  ライブラリパネルの新しいトグルスイッチ。トラックをアームした状態でONにすると、そのトラックを基準にライブラリ全体を「次にミックスすべき曲」順に並べ替え、マッチ度(0-100%)とその理由("same key" "BPM close" など)を表示する。スコアリングはBPM近接度(半分/倍テンポも考慮)・Camelotキー距離・エナジー差分を組み合わせたルールベースのローカル演算(`backend/harmonic.py: score_pair`)で、クラウド呼び出しやモデルのダウンロードは一切なし。相性の低い候補は自動的に薄く表示される。
  Gemini Nano等の実ブラウザ内蔵AIモデルを使う拡張の余地も残してあるが、今回は「常に安定して動く」ことを優先し、透明性のあるヒューリスティックのみで実装した。

- **ホットキュー(rekordbox的なメモリーキュー)**
  ライブラリ項目の波形をクリックすると、その位置にキューポイントを追加(1トラックあたり最大8個)。波形上にオレンジのティックマークとして表示され、右クリックで最も近いキューを削除できる。キューはトラックごとの小さなピルボタンとしても並び、クリックするとそのトラックをアーム + 次に配置するクリップの開始位置(source_offset)をそのキュー位置にセットする。キューは解析キャッシュとは別データとして保持されるため、再解析(BPM/キーの手動修正など)しても消えない。

### 変更・内部

- `backend/analysis.py`: `ANALYSIS_VERSION` を導入。解析結果のスキーマが変わった際に、キャッシュ済みライブラリを自動的に再解析させる仕組みを追加。
- `backend/library.py`: キャッシュエントリのバージョンチェック、ホットキューのCRUD(`add_cue` / `remove_cue`)、再解析時にキューを保持する `force_reanalyze` を追加。
- `backend/main.py`: `POST /api/library/cue/add`, `POST /api/library/cue/remove`, `POST /api/library/suggest` を追加。

### 既知の制約(追加分)

- エナジー値はRMSベースの簡易指標で、正式なラウドネス/ダイナミクス解析ではない。
- AI Mix Assistantはライブラリ全体をメモリ上でスコアリングするため、非常に大きなライブラリ(数千曲規模)では応答が遅くなる可能性がある。
- ホットキューの並び替え(ドラッグでの時間調整)は未実装。追加/削除のみ。

---

## 2026-09-03以前

- 初期リリース: Ableton的な複数デッキ + タイムライン打ち込み、rekordbox的なライブラリ解析(BPM/キー/波形)、リバーブセンド、オフラインWAVレンダリング。詳細は README を参照。
