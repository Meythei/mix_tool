# Changelog

DJ Mix Studioのパッチノート。日付は変更をまとめた日(JST)。

## 2026-09-03 — ハーモニックミキシング・試聴・AIアシスト・Undo/Redo

Ableton LiveとrekordboxをつなぐDJミックス制作ツールとして、rekordbox寄りの機能を中心に補強。

### 追加

- **Camelot Wheel対応**: `backend/analysis.py`のキー解析結果から、rekordbox/Mixed In Key互換のCamelotコード(例: `8A`)を自動算出。ライブラリ一覧に色分けチップとして表示し、コード番号が近いほど色相が近くなるので隣接キーがひと目でわかる。
- **試聴プレイヤー**: ライブラリ項目の▶ボタン、または波形クリックで、タイムラインに置かずその場で音源を再生できる新しい`/api/library/audio`エンドポイント経由の試聴機能。フル解像度波形・再生位置・現在/全体時間を表示。
- **ホットキュー(メモリーキュー)**: 試聴中に数字キー1〜4で最大4つのキューポイントを打ち込み、既に打ってあるキーで即ジャンプ(Shift+数字でクリア)。`/api/library/cues`でライブラリキャッシュに永続化されるので、次回起動後も残る。rekordboxのホットキューに相当。
- **AI Mix Assistant(✨ Suggest compatible tracks)**: アーム中/選択中のクリップを基準に、Camelot調和距離(同一・隣接・相性の良い相対調ほど高スコア)とBPM適合度(同一・倍テンポ・半分テンポなど)をローカルで採点し、ライブラリから合う曲を上位順に提案するパネルを追加。外部APIやモデルのダウンロードなしに、ブラウザ内の解析済みメタデータだけで完結する軽量なオンデバイス推薦ロジック。
- **Undo / Redo**: クリップ移動・トリム、オートメーション打ち込み、デッキの追加/削除、各種つまみの変更などの操作をひとつのコミット単位としてスナップショット。ツールバーの⟲⟳ボタン、または`Ctrl+Z` / `Ctrl+Shift+Z`(`Ctrl+Y`でもRedo)でAbleton Live同様に多段階のやり直しが可能に。

### 変更

- `library`のキャッシュエントリのスキーマに`camelot`(文字列 or null)と`cues`(`{time, label}`の配列、任意)を追加。既存のキャッシュファイルは次回スキャン/解析時に自動的に補完される(後方互換、必須フィールドではない)。

### 内部変更

- `backend/main.py`に`GET /api/library/audio`(既知のライブラリパスのみ配信するガード付き)と`POST /api/library/cues`を追加。
- `frontend/app.js`の`scheduleSync()`を全ての意味のあるコミットの単一の合流点として使い、そこにUndo/Redoのスナップショット記録をフックすることで、個々のミューテーション箇所を書き換えずに履歴機能を追加。
