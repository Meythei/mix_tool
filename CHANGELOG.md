# パッチノート

## 2026-09-03 — ハーモニックミキシング + ライブラリブラウザ強化

Ableton(打ち込み)× rekordbox(ライブラリ管理・キー解析)のうち、rekordbox側で
まだ足りていなかった「ライブラリブラウザとしての実用性」を強化。

### 追加

- **Camelotキー表記**: 解析結果のキー(例: `A minor`)から Camelot Wheel コード
  (例: `8A`)を算出し、ライブラリの各トラックにチップ表示。`backend/analysis.py`
  の `key_to_camelot()` / `camelot_compatible()`。
  - 既存キャッシュ(`library_cache.json`)には `camelot` フィールドが無いため、
    フォルダを再Scanすると自動的に埋まる(ファイルが変わっていなくても
    再解析が必要な場合は「Reanalyze」相当の再Scanで反映)。
- **ライブラリの検索・並べ替え**: サイドバーに検索ボックス(ファイル名の部分一致)
  と並べ替え(Name / BPM / Key)を追加。rekordboxのブラウザ相当の基本操作。
- **Compatible only フィルタ**: 選択中のクリップ(または armed 中のライブラリ項目)
  を基準に、Camelotキーが互換(同一・relative major/minor・ホイール上で隣接)かつ
  BPMが近い(半々/倍速テンポも許容)トラックだけに絞り込むトグル。互換トラックは
  ライブラリ上でも緑枠(tertiary色)でハイライト。
- **Mix Assistant パネル(Inspector下部)**: 選択中のクリップ/armedトラックを基準に、
  Camelot互換度(60%)+BPM近似度(40%)の加重スコアでライブラリ全体をランキングし、
  次に繋ぐ候補を最大6件提案。クリックでそのままarm可能。
  - あくまでローカルのヒューリスティック(ルールベース)スコアリングで、実際の
    ローカルLLM/MLモデルは使用していない。将来 Gemini Nano 級のオンデバイスモデルに
    差し替えられるよう、スコアリング関数(`rankSuggestions()` in `frontend/app.js`)を
    UIから疎結合にしてある。

### 変更なし(既存機能への影響)

- レンダリングエンジン・プロジェクトスキーマ(`models.py`)・保存フォーマットは無変更。
  既存プロジェクトJSONはそのまま読み込み可能。
- ライブラリAPI(`GET /api/library` 等)のレスポンスに `camelot` フィールドが追加
  されただけで、既存フィールドの意味・型は変更していない。

### 動作確認

- `scripts/smoke_test.py`: 解析→レンダリングの一気通貫を確認(既存動作に回帰なし)。
- Playwrightでブラウザを起動し、検索/並べ替え/Compatible onlyトグル/Mix Assistant
  の提案生成/クリップ選択との連動をUI上で確認(コンソールエラーなし)。

### 既知の制約 / 今後

- BPMがピッチ推定できない音源(`bpm: null`)はBPMスコアが常に0になり、Mix
  Assistantでは低スコアに沈む(Camelotキー一致分の0.6のみで並ぶ)。
- Mix Assistant は現状ライブラリ全件をクライアント側で毎回スコアリングしている
  (数千曲規模のライブラリでは軽量な絞り込みや事前ソートを検討の余地あり)。
- 次点候補: ホットキュー/ビートグリッドマーカー(rekordbox方式)、クリップの
  波形上での直接トリム(ソース全体波形を見ながらドラッグ)、複数プロジェクトの
  同時比較。
